.gradle/
.kotlin/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
*.iws
*.ipr
out/
.vscode/

# Local configuration
local.properties
.DS_Store

# Xcode — generated by `cd iosApp && xcodegen generate` from iosApp/project.yml,
# which is the source of truth. The .xcodeproj is a merge-conflict machine and
# is deliberately not committed; neither is the Info.plist XcodeGen writes from
# the `info:` block in that spec.
*.xcodeproj/
xcuserdata/
DerivedData/
iosApp/HouseholdHub/Info.plist
