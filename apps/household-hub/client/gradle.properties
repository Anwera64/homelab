org.gradle.jvmargs=-Xmx4096M -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# Reuse task outputs across builds and CI jobs when their inputs haven't changed.
org.gradle.caching=true

# The hub every client build talks to. Generated into :core:data's BuildConfig.
# Point a build elsewhere with -Phub.baseUrl=... or your user-level gradle.properties.
hub.baseUrl=https://hub.spicy-llama.duckdns.org
